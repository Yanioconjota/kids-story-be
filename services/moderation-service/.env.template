PORT=8001

# Provider: mock | rules | openai  (default: mock)
# mock  = always approved (dev/tests)
# rules = local keyword blocklist, no API key needed
# openai = OpenAI Moderation API (requires MODERATION_API_KEY)
MODERATION_PROVIDER=mock

# Required only for MODERATION_PROVIDER=openai
MODERATION_API_KEY=
